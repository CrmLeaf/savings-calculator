name: Incorrect calculation
description: Payroll Savings Calculator returns a figure you believe is statutorily wrong.
title: "[calc] "
labels: ["bug", "calculation"]
body:
  - type: markdown
    attributes:
      value: |
        A wrong statutory figure is the most important kind of bug this package
        can have, so thank you for reporting it.

        The single most useful thing you can give us is **the rule that says we
        are wrong**. Inputs plus expected output plus a citation becomes a test
        case, and a test case becomes a fix.

        This tool implements:

        > Not a statutory calculation. It compares two per-employee-per-month prices over a contract term, including platform fees and the one-off cost of migrating, so the answer is a total cost of ownership rather than a headline rate.

  - type: input
    id: version
    attributes:
      label: Package version
      placeholder: "crmleaf/savings-calculator 1.0.0"
    validations:
      required: true

  - type: input
    id: financial-year
    attributes:
      label: Financial year the figure applies to
      placeholder: "2025-26"
    validations:
      required: true

  - type: textarea
    id: inputs
    attributes:
      label: Inputs
      description: Every value you passed. Code is ideal.
      render: php
      placeholder: |
        (new SavingsCalculator())->calculate(
        employeeCount: 250,
        currentPerEmployeePerMonth: Money::fromRupees(120),
        proposedPerEmployeePerMonth: Money::fromRupees(75),
        currentAnnualPlatformFee: Money::fromRupees(60_000),
        );
    validations:
      required: true

  - type: input
    id: actual
    attributes:
      label: Figure you got
    validations:
      required: true

  - type: input
    id: expected
    attributes:
      label: Figure you expected
    validations:
      required: true

  - type: textarea
    id: citation
    attributes:
      label: Statutory basis for the expected figure
      description: >
        The section, notification, circular or gazette entry. A link is perfect.
        If you are working from an accountant's advice or a filing that was
        accepted, say so - that is still useful evidence, just tell us which it is.
    validations:
      required: true
